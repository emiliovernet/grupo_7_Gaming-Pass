<%- include('partials/head')  %> 
    <link rel="stylesheet" href="/css/home.css">
    <title>Gaming Pass</title>
 </head>

<body>
    
    <%- include('partials/header') %>

    <section class="banner">
        <img src="/images/IMG-BANNER1.JPG" alt="Banner de ofertas">
    </section>

    <main>

    <section class="Notebooks">
        <h3>Notebooks</h3>
        <div class="products">
            
            <% products.forEach((product)=>{ %> 
                <% if(product.product_categories_id == "1") { %> 
            <article>
            <a href="/detail/<%= product.id %> "><img src="/images/<%= product.images %> " width="360px" alt="<%= product.description %> "></a>
                <div class="main-article">
                    <i class="fas fa-truck truck-icon"></i>
                    <p class="price">$<%= product.price %> </p>
                    <p class="discount"><%= product.discount %> % off</p>
                    <h4 class="description"><%= product.description %> </h4>
                </div>
            </article>
            <%}}) %> 
</section>

<section class="banner">
    <img src="/images/IMG-BANNER2.JPG" alt="Banner de ofertas">
</section>

<section class="Monitores">
    <h3>Monitores</h3>
    <div class="products">
        <% products.forEach((product)=>{ %> 
            <% if(product.category == "monitor") { %> 
        <article>
        <a href="/detail/<%= product.id %> "><img src="/images/<%= product.image %> " width="360px" alt="<%= product.description %> "></a>
            <div class="main-article">
                <i class="fas fa-truck truck-icon"></i>
                <p class="price">$<%= product.price %> </p>
                <p class="discount"><%= product.discount %> % off</p>
                <h4 class="description"><%= product.description %> </h4>
            </div>
        </article>
        <%}}) %> 
</section>

<section class="memory">
    <h3>Memorias</h3>
    <div class="products">
        <% products.forEach((product)=>{ %> 
            <% if(product.category == "memory") { %> 
        <article>
        <a href="/detail/<%= product.id %> "><img src="/images/<%= product.image %> " width="360px" alt="<%= product.description %> "></a>
            <div class="main-article">
                <i class="fas fa-truck truck-icon"></i>
                <p class="price">$<%= product.price %> </p>
                <p class="discount"><%= product.discount %> % off</p>
                <h4 class="description"><%= product.description %> </h4>
            </div>
        </article>
        <%}}) %> 
</section>

<section class="banner">
    <img src="/images/IMG-BANNER3.JPG" alt="Banner de ofertas">
</section>

<section class="beneficies">

        <div class="items">
        <article>
            <div class="item1">
                <i class='fa fa-truck-moving'></i>
                <h4 class="text">Envios a todo el pais y garantias</h4>
            </div>
        </article>

        <article>
            <div class="item2">
                <i class="fa fa-credit-card"></i>
                <h4 class="text">Paga con todos los medios de pago</h4>
            </div>
        </article>
    </div>
</section>
</main>


<%- include('partials/footer') %>
